<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://bootswatch.com/4/slate/bootstrap.min.css">
  <title>Redis TODO List</title>
</head>
<body>
  <div class="container">
    <h1 class="text-center"> <%= title %> </h1>
    <form action="/todo/add" method="POST">
      <div class="form-group">
        <input type="text" class="form-control" name="todos" placeholder="Add a new task...">
        <input type="submit" value="Add" class="btn btn-primary">
      </div>
    </form>
    <form action="/todo/delete" method="POST">
      <% todo.forEach((taskObj) => { %>
        <div class="alert alert-success">
          <!-- Ensure the checkbox name is todo[] to send as an array -->
          <input type="checkbox" class="form-check-input mt-2" name="todo[]" value="<%= taskObj.id %>">
          <label class="form-check-label"><%= taskObj.task %></label>
        </div>
      <% }) %>
      <input type="submit" value="Remove" class="btn btn-primary">
    </form>    
    <br>
  </div>
</body>
</html>
